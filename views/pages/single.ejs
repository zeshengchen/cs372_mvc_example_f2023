<% if (success.length > 0) { %>
    <div class="alert alert-success"><%= success %></div>
<% } %>

<div class="jumbotron text-center">
    <h1><%= post.name %></h1>
    <p><%= post.description %></p>
</div>
